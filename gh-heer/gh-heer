#!/bin/bash
# gh-heer — GitHub CLI extension for multi-provider LLM authentication

set -e

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Execute the Node.js CLI
node "${SCRIPT_DIR}/index.js" "$@"
